{
  "_comment": "이 파일을 tracker-settings.json 으로 복사한 뒤 telegramBotToken / telegramChatId 를 채우세요. tracker-settings.json 은 .gitignore 로 커밋되지 않습니다 (토큰 보호).",

  "idleMinutes": 60,
  "_idleMinutes": "이 시간(분) 이상 연속 무활동 + CPU 임계 미만이면 idle(버려진 세션)로 판정. 활성 세션은 1시간 내 반드시 CPU 를 쓰므로 오탐 방지의 핵심.",

  "cpuThresholdPct": 0.5,
  "_cpuThresholdPct": "추적 간격 동안 CPU 사용률이 이 % 이상이면 '활동'으로 보고 lastActive 를 갱신. 미만이면 idle 누적.",

  "trackIntervalMin": 5,
  "_trackIntervalMin": "백그라운드 추적(스냅샷) 주기(분). install-launchd.sh 가 이 간격으로 --track-activity LaunchAgent 를 등록.",

  "minAgeMinutes": 30,
  "_minAgeMinutes": "시작한 지 이 시간(분) 미만인 프로세스는 idle/고아 어느 경로로도 종료 후보가 되지 않음 (방금 연 세션 보호).",

  "minFreePct": 25,
  "_minFreePct": "--idle-only 스윕 시 가용 RAM 이 이 % 이상이면 정리를 통째로 생략 (여유 있는데 죽일 이유 없음).",

  "protected": [],
  "_protected": "커맨드라인 부분 일치 패턴 배열. 매칭되는 프로세스는 절대 자동 종료하지 않음. 예: [\"my-always-on-listener\", \"/path/to/service/\"]",

  "alert": {
    "enabled": false,
    "telegramBotToken": "",
    "telegramChatId": "",

    "ramPctThreshold": 10,
    "_ramPctThreshold": "idle/고아 프로세스 메모리 합계가 전체 RAM 의 이 % 이상이면 알림 (OR 조건).",

    "idleCountThreshold": 10,
    "_idleCountThreshold": "idle/고아 프로세스 개수가 이 값 이상이면 알림 (OR 조건).",

    "idleMemMBThreshold": 4096,
    "_idleMemMBThreshold": "idle/고아 프로세스 메모리 합계 절대값(MB) 임계 (OR 조건).",

    "cooldownMin": 30,
    "_cooldownMin": "직전 알림 이후 이 시간(분) 안에는 재알림 안 함 (도배 방지)."
  }
}
