# 2026-07-05 — 자동 정리 주기/텔레그램 안내 문구 정렬

## 변경
- `Cleanup-Schedule.ps1` 기본 자동 정리 간격을 2시간으로 변경.
- 텔레그램 알림 문구를 실제 운영 방식에 맞춰 수정:
  - 자동 정리: `ClaudeCodeMemoryCleanup` 작업이 2시간마다 `-IdleOnly` 실행.
  - 즉시 정리: `Run-IdleCleanup.bat`.
- `Track-Schedule.ps1`, `README.md`의 안내도 같은 표현으로 정렬.
- `Test-Patterns.ps1`에 자동 정리 기본 2시간 + 텔레그램 안내 동기화 회귀 체크 추가.

## 확인
- 현재 등록된 `ClaudeCodeMemoryCleanup` 작업은 작업 스케줄러상 `PT3H`였음.
- 일반 권한 셸에서 실제 작업 재등록/변경은 모두 `Access is denied`로 실패:
  - `Register-ScheduledTask ... -RunLevel Highest`
  - `schtasks /Change /TN ClaudeCodeMemoryCleanup /RI 120`
- 이후 UAC/elevated PowerShell로 직접 `Register-ScheduledTask` 재등록 성공.
- 실제 운영 작업도 `PT2H`로 변경됨.

## 검증
- `Test-Patterns.ps1` 출력에 `[FAIL]` 없음.
- 전체 PowerShell AST parse 통과.
- `git diff --check` 통과.

## 추가 변경 — 자동 정리 3시간 + 메모리 압박 알림
- 사용자 운영 요청에 맞춰 자동 정리 기준을 2시간에서 3시간으로 완화.
  - 라이브 작업 스케줄러 `ClaudeCodeMemoryCleanup` 반복 간격: `PT3H`.
  - `Cleanup-Schedule.ps1` 기본값도 `IntervalHours = 3`으로 변경해 재등록 시 3시간을 유지.
  - `MemoryReset.ps1`, `Track-Schedule.ps1`, `README.md`, `Test-Patterns.ps1`의 안내/회귀 체크를 3시간 기준으로 동기화.
- `tracker-settings.json` 런타임 설정을 메모리 압박 중심으로 조정.
  - `idleMinutes`: `180`
  - `alert.idleCountThreshold`: `999`
  - `alert.idleMemMBThreshold`: `8192`
  - `alert.ramPctThreshold`: `15`
  - `alert.cooldownMin`: `180`
  - 텔레그램 토큰/chat_id는 유지하고 파일은 `.gitignore` 보호 상태 확인.
- `MemoryReset-Tray.ps1` 트레이 데몬 재시작.
  - 새 상주 PID: `75160`
  - `tray-debug.log`에서 메시지 루프 진입 확인.

## 추가 검증
- 구조 확인: 알림 기준은 `Get-TrackerSettings` → `Invoke-ActivityTracking`, 정리 주기는 `Cleanup-Schedule.ps1` → 작업 스케줄러 반복 간격으로 제어됨. 기존 제어 지점 사용이므로 임시 우회가 아님.
- 적대적 검토: 서브에이전트가 repo 기본값 2시간 잔존을 blocker로 지적했고, 기본값/문서/테스트를 3시간으로 보완.
- `Test-Patterns.ps1` 통과.
- 전체 PowerShell AST parse 통과.
- `git diff --check` 통과.
- 2시간 잔존 문구 검색 결과 없음.
- `ClaudeCodeMemoryTracker` 수동 tick 실행 결과:
  - `LastTaskResult`: `0`
  - `tracker.log`: `candidates=0 memMB=0 ramPct=0% trigger=False alertEnabled=True`
